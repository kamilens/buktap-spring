spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    database: mysql
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://localhost:3306/buktapdb?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: buktap
    password: GnkuKw5d
    driverClassName: com.mysql.cj.jdbc.Driver

buktap:
  file-storage-path:
    books:
      main-path: "C:/temp_storage/books"
      pdf-pages-suffix: "/pdf_pages"
      book-images-suffix: "/book_images"